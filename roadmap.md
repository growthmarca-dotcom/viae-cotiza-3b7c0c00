# Roadmap de trabajo

## Hecho
- [x] Intervención 1 — Ciclo de estados de `quotations` (draft → sent → accepted/rejected/expired), guarda de transiciones, expiración perezosa, filtros y dashboard con estados reales, Smart Quotes fuera del menú.

## En curso
- [x] Vista pública de cotización (`/cotizacion/$token`), solo estos 3 puntos:
  - [x] Mostrar la URL de la cotización (dinámica, copiable, enlazable).
  - [x] Botón "Contactar agente" con ícono de WhatsApp y mensaje inicial de la cotización.
  - [x] Enlace inferior a la web oficial de VIAE, clickeable y en pestaña nueva (mismo estilo/ubicación).

- [x] Intervención 2 — Aceptación/rechazo público por token: respuesta del cliente desde `/cotizacion/$token` (aceptar/rechazar + comentario opcional), una sola vez, respetando vencimiento y guarda de transiciones; visible en la ficha interna.

- [x] Intervención 3 — Conversión Cotización → Reserva completa: idempotencia (una cotización = una reserva, índice único + reuso de la reserva existente) y alta de acompañantes según `pax_count` además del titular. El traslado `quotation_items → booking_services` y titular → `booking_passengers` ya estaba operativo.

- [x] Intervención 4 — Membresías: panel "Miembros y accesos" en la ficha de organización (listado con identidad, cambio de rol interno, revocación de acceso), invitaciones por correo con enlace copiable y revocación, y ruta de aceptación `/invitacion/$token` con sesión requerida. Función segura `list_organization_members`. Sin tocar Intervenciones 1-3.

## Pendiente
- [x] Intervención 5 — Economía / Tipo de cambio: carga de tipos de cambio del Financial Core en Ajustes (`CurrencyRatesCard` + RPC `register_currency_exchange_rate`, histórico inmutable) y sello de la tasa aplicada en la conversión a reserva (`applied_exchange_rate` / `applied_rate_date` / `applied_rate_source` en `bookings` y `booking_services`; prioridad manual → snapshot → sin tasa). Las reservas no cambian de importe si luego se carga otra tasa.
- [x] Notificación al agente cuando el cliente responde la cotización: trigger `tg_quotation_client_response_notify` en `quotations` (transición sin responder → respondida) que crea avisos en el centro de notificaciones existente para el agente asignado a la oportunidad y/o el dueño de la cotización, con número, cliente, fecha/hora, comentario y enlace directo; índice único por (usuario, cotización, tipo) para idempotencia; si no hay agente responsable queda registrado en `audit_log` sin afectar la respuesta del cliente. La campana distingue "Cliente aceptó" / "Cliente rechazó" y ofrece "Abrir cotización". No crea reservas.
- [x] Intervención 7 — Conversión **asistida** de cotización aceptada → reserva: la acción "Convertir a reserva" aparece en la ficha interna solo cuando `quotations.status = 'accepted'` y no existe reserva; si ya existe muestra "Reserva creada · Abrir VIA-…". Diálogo de confirmación de solo lectura (`QuotationConvertDialog`) con cliente, número de cotización, destino, fechas, pasajeros, cantidad de servicios, importe y moneda, y acción final "Confirmar conversión". Reutiliza `createBooking()` (idempotente por `bookings.quotation_id`, traslado de `quotation_items → booking_services` y titular/acompañantes → `booking_passengers`, herencia de contexto comercial y sello de tipo de cambio). `createBooking()` ahora rechaza el origen cotización si el estado no es `accepted`. Trazabilidad con trigger `tg_quotation_converted_to_booking`, que agrega el evento `converted_to_booking` en `quotation_history` con reserva, número y fecha.
- [x] Intervención 8 — Cierre del ciclo comercial en el Pipeline: al convertir una cotización aceptada en reserva, la oportunidad asociada se cierra como ganada reutilizando `moveOpportunityStage()` y la etapa del grupo `won` de `opportunity_stage_config` (hoy `booked`). Idempotente: si ya está ganada no vuelve a mover la etapa ni a escribir `opportunity_history`; la reserva sigue siendo única por `bookings.quotation_id`. Si la reserva se crea pero el cierre falla, se registra en `audit_log` (`pipeline_close_failed`, RPC `log_pipeline_close_issue`), se avisa al agente y se puede reintentar con "Cerrar oportunidad como ganada" desde la ficha de cotización, sin crear otra reserva. Rechazo del cliente: no cierra nada automáticamente; acción asistida "Marcar oportunidad como perdida" con motivo obligatorio (`opportunities.lost_reason`) y confirmación, disponible en la ficha de cotización y de oportunidad.
- [ ] Conversión automática a reserva al aceptar — DESCARTADA POR DECISIÓN DE PRODUCTO (no es un pendiente técnico).
- [x] Comisiones v1.9.4 **Fase B1** — devengo real: RPC `accrue_commission(booking_service_id)` y `accrue_booking_commissions(booking_id)` (`SECURITY DEFINER`, solo administrador) que reutilizan `resolve_agreement()` + `compute_commission()` y persisten en `commissions` con `status = 'accrued'`, snapshots de acuerdo/regla, TC sellado (`exchange_rate`, `exchange_rate_date`, `exchange_rate_source` desde `booking_service_economics`) y `computed_by`. Hecho económico = reserva no pendiente ni cancelada (`confirmed`/`reserved`/`voucher_issued`/`in_progress`/`completed`); el pipeline y las cotizaciones NO disparan devengo. Idempotencia sobre el índice existente `commissions_unique_effective` (precheck + tolerancia a `unique_violation`). Historial automático por trigger `commissions_history` → `commission_history` (`NULL → accrued` y cada cambio de estado, con actor y timestamp). Transiciones controladas por `set_commission_status()`: `accrued → approved`, `accrued/approved → cancelled`; `settled` bloqueado; sin DELETE. Sin liquidaciones, pagos ni UI nueva (corresponde a B2).
- [x] Comisiones v1.9.4 **Fase B2** — primera versión operativa: configuración real de acuerdos y reglas (`AgreementRulesDialog` sobre `agreement_rules`, activar/desactivar acuerdos desde `/agreements`), panel "Comisiones devengadas" en `/bookings/$id` (devengo por reserva o servicio vía RPC, aprobación/cancelación exclusivamente por `set_commission_status()`, snapshots de acuerdo/regla, TC, base calculada solo para administrador, historial leído de `commission_history`) y vista de consulta `/commissions` ("Mis comisiones", recorte por RLS, sin costos ni márgenes). Hardening en `0012_commissions_hardening_phase_b2.sql`: `tg_commission_immutable` rechaza todo DELETE y cualquier cambio económico/de trazabilidad cuando el estado es `accrued`/`approved`/`cancelled` (solo estado, notas y `updated_at`), `commission_history` append-only por trigger, y revocación de `DELETE` en `commissions` y `DELETE/UPDATE` en `commission_history` para `authenticated`. El motor sigue siendo único (`resolve_agreement()` + `compute_commission()`); `agents.commission_*` NO se usa para calcular. Sin liquidaciones ni pagos (Fase C) y sin automatización por estado de reserva ni por pipeline.
- [x] Comisiones **Fase C1.1** — Núcleo de liquidaciones (`0013_commission_settlements_phase_c11.sql`): configuración mínima en el acuerdo (`settlement_delay_days` 0–365 y `settlement_frequency` mensual/quincenal con períodos fijos 1–15 y 16–fin de mes) y autorización explícita del beneficiario agente (`agents.settlement_authorized`, que NO participa del cálculo). Nuevas tablas `commission_settlements`, `commission_settlement_items` (snapshot del importe, fecha de servicio y fecha de elegibilidad) y `commission_settlement_history` (append-only). RPC `generate_commission_settlements(_as_of)` solo administrador: toma comisiones `approved` con servicio finalizado (`booking_services.service_date`, si falta `bookings.travel_end`) y plazo del acuerdo cumplido, y las agrupa por beneficiario + moneda + período; idempotente (índice único por comisión, nunca la incluye dos veces) y sin sumar monedas distintas. Estados `draft → pending_review → approved` por `set_settlement_status()`; `settled` reservado para C1.2 y bloqueado. Integridad: una liquidación aprobada no se elimina ni admite cambios económicos ni cambios en su detalle; historial append-only; notas solo por RPC. UI administrativa `/settlements` (generación, totales por moneda, filtros por estado/moneda/beneficiario) y `/settlements/$id` (detalle con comisiones incluidas, transiciones, notas e historial). No calcula comisiones: consume las devengadas por el motor existente. Sin facturas, documentación fiscal, pagos ni comprobantes (C1.2).
- [x] Comisiones **Fase C1.2** — Facturación, documentación y registro de pago (`0015_commission_settlement_c12_core.sql`, `0016_commission_settlement_c12_rpcs.sql`): nuevos estados `invoice_pending → invoice_review → ready_for_payment → settled` sobre los de C1.1. Tablas `commission_settlement_documents` (facturas con versionado por reintento, metadatos fiscales, importe y moneda, motivo de rechazo, actores y fechas; una sola factura aprobada por liquidación) y `commission_settlement_payments` (un único pago por liquidación, escritura solo administrativa). Archivos en el bucket privado `commission-documents` (PDF/JPG/PNG, máximo 5 MB), subida y URLs firmadas de corta duración por server functions autorizadas (`src/lib/settlement-files.functions.ts`). RPCs `submit_settlement_invoice()` (beneficiario o administrador, valida moneda e importe), `review_settlement_document()` (solo administrador; el rechazo exige motivo y devuelve la liquidación a factura pendiente; la aprobación la deja lista para pago) y `record_commission_settlement_payment()` (solo administrador, atómico: exige factura aprobada, importe exactamente igual al total y misma moneda, sin pagos parciales ni múltiples, y deja la liquidación `settled`). Inmutabilidad: la factura aprobada y el pago no se modifican ni se eliminan; historial append-only con `invoice_submitted`, `invoice_rejected`, `invoice_approved` y `payment_recorded`. UI: paneles de factura y pago en `/settlements/$id`, nueva vista del beneficiario `/my-settlements` (recorte por RLS) y filtros por los nuevos estados. Sin conversiones de moneda, pagos automáticos, conciliación bancaria ni ajustes (C1.3).
